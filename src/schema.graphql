type Query{
    film(id:String): Film!,
    films(search:String):[Film!]!
}

type Film{
    imdbID: String!,
    title:String!
    poster: String,
    actors: [Actor],
    type: String,
    released: String
}

type Actor{
    name: String!
}